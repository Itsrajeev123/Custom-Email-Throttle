# Custom-Email-Throttle
A lightweight Odoo module that throttles outgoing emails by pushing them into the mail queue and sending them at a controlled rate (e.g., 1 email per minute) to prevent server overload and SMTP limits.
